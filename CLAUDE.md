# Portfolio Project — Claude Rules

## Git
- Never work directly on main. All work happens on feature branches and merges into main via PR.

## Agents
- For any content, copy, or case study work, read `agents/writer.yml` before starting.
- For any code, component, or styling work, read `agents/developer.yml` before starting.
