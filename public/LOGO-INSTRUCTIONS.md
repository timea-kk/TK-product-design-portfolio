# Logo

**Put your logo here.**

1. Place your logo file in this folder (`public/`).
2. Name it **`logo.svg`** so the site can find it.

The site header will use it automatically. If the file is missing, a text fallback (your name or “TK”) will show instead.
